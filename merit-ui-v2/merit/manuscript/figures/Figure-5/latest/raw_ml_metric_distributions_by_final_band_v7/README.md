# Raw ML Metric Distributions by Final Readiness Band

Source cache: `merit-cache-workbench-full-v7`

Unit: one study-level v7 workflow state from `index.json` (`n=4,121`).

The figures show raw quantities, not normalized MERIT metric scores.

Main figure panels: biological sample count, smallest class size, number of classes, feature count, p/n ratio, sample-level missingness, feature-level missingness burden, and class imbalance.

Secondary panels: total samples, usable matrices, worst p/n ratio, class-dependent missingness, sample outliers, feature redundancy, label entropy, and label coverage.

Focused main plot: biological sample count, smallest class size, p/n ratio, and median sample-level missingness.
